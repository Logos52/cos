# Focus

Building cos — the terminal-first Textual home.

- cos is the operating surface; the llm-knowledge-base vault stays the durable thinking layer, read-only to automation
- Data layer stays plain + model-agnostic (files / JSON, any runtime)
- Ship read-only panels before actions

<!-- Human-maintained. The cos home reads this read-only; edit the headline + bullets
     whenever your focus shifts. First non-heading line = headline; "- " lines = bullets. -->
