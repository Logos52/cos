# Reading Goals

## Active topics (priority order)

1. **Agentic engineering** — tool use, memory architectures, eval harnesses, multi-agent patterns
2. **Learning techniques / ICS** — spaced repetition, active recall, knowledge consolidation
3. **Textual** — Python TUI framework, reactive attributes, layouts (for `cos` future frontend)

## What "done" means

An item is done when it's been read and either: (a) key points captured to the vault, or (b) explicitly marked as not worth capturing.

## Backlog target

Keep unread queue under 20 items. Triage weekly — archive or drop stale items.
